# Setup

Copy everything into:

`Haideransari444/Haideransari444`

Then run:

```bash
git add .
git commit -m "add cinematic farm profile v3"
git push
```

Then go to GitHub Actions and manually run:

1. Refresh Cinematic Farm Header
2. GitHub-Profile-3D-Contrib
3. Generate Contribution Snake

Header switching uses Pakistan time:

- 05:00 to 08:59 = dawn
- 09:00 to 16:59 = day
- 17:00 to 19:59 = dusk
- 20:00 to 04:59 = night

Music note:
GitHub README cannot reliably autoplay audio. The README uses a custom play button.
